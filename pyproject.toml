[project]
name = "china-ets-mcp"
version = "1.0.0"
description = "MCP server for China ETS (CEA/CCER) carbon market data — dashboard, query, and export tools"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{name = "JIN-Z-pop and his merry AI brothers"}]
dependencies = [
    "mcp[cli]>=1.0",
    "openpyxl>=3.1",
    "jinja2>=3.1",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
china-ets-mcp-lite = "china_ets_mcp.server_lite:main"
china-ets-dashboard = "china_ets_mcp.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
addopts = "-p no:flask"
